# Suggested commands
- Build: `cargo build`
- Run dev server: `cargo run` (listens on 0.0.0.0:3000; Swagger at /swagger-ui)
- Tests: `cargo test`
- Format: `cargo fmt`
- Lint: `cargo clippy --all-targets --all-features`
- Common tooling: `ls`, `find`, `grep` (macOS/Darwin default); git for VCS.